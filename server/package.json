{
  "name": "dockcheck-local-server",
  "version": "1.3.62",
  "description": "Dock Check Local Server — API REST + SQLite + ControlID Agent + Cloud Sync",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  }
}
