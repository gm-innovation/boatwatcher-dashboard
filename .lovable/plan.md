

## Ajustes visuais: StatisticsCards e ProjectInfoCard

### Alterações

1. **`src/components/dashboard/StatisticsCards.tsx`** (linha 17):
   - Reduzir padding de `p-6` para `p-4` e gap de `gap-3` para `gap-2`
   - Reduzir ícone de `p-3` para `p-2` e tamanho do valor de `text-3xl` para `text-2xl`

2. **`src/components/dashboard/ProjectInfoCard.tsx`**:
   - Linha 33: remover `border` do estado vazio
   - Linha 48: remover `border` do card principal, adicionar `shadow-sm`

